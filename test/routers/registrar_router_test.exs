defmodule RegistrarRouterTest do
  use ExUnit.Case

  # Sometimes it may be convenient to test a specific
  # aspect of a router in isolation. For such, we just
  # need to set the @endpoint to the router under test.
  @endpoint RegistrarRouter

  # test "returns OK" do
  #   conn = get("/")
  #   assert conn.status == 200
  # end

  # test "headers returns OK" do
  #   conn = get("/headers")
  #   assert conn.status == 200
  # end
end
